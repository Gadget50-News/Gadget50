## Admin diagnostics

After installation, sign in as Super Admin and open:

```text
admin/diagnostics.php
```

The page checks PHP version, required extensions, database connectivity, sessions, writable paths and email-service status without displaying passwords.
